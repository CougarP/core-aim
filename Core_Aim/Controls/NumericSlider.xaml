<UserControl x:Class="Core_Aim.Controls.NumericSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="PhantomNumBtn" TargetType="Button">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Background" Value="#FF03070F"/>
            <Setter Property="Foreground" Value="#FF506070"/>
            <Setter Property="BorderBrush" Value="#FF1E2E42"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="{StaticResource MonoFont}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#1A00F0FF"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="#FF00F0FF"/>
                                <Setter Property="Foreground" Value="#FF00F0FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#3300F0FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>

        <Button x:Name="BtnMinus" Grid.Column="0" Content="−"
                Style="{StaticResource PhantomNumBtn}"
                Click="BtnMinus_Click"/>

        <Slider x:Name="MainSlider" Grid.Column="1"
                Minimum="{Binding Minimum, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Maximum="{Binding Maximum, RelativeSource={RelativeSource AncestorType=UserControl}}"
                SmallChange="{Binding SmallChange, RelativeSource={RelativeSource AncestorType=UserControl}}"
                LargeChange="{Binding LargeChange, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Value="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                Foreground="{Binding Tint, RelativeSource={RelativeSource AncestorType=UserControl}}"
                VerticalAlignment="Center"
                Margin="6,0"/>

        <Button x:Name="BtnPlus" Grid.Column="2" Content="+"
                Style="{StaticResource PhantomNumBtn}"
                Click="BtnPlus_Click"/>
    </Grid>
</UserControl>
