<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ═══════════════════════════════════════════════════════════════
         PHANTOM BUTTON — spec §6
         Diagonal clip on 2 corners, shimmer Rectangle SkewX -18°
         animated TranslateX in 3s loop, electric border + 7% bg.
         ═══════════════════════════════════════════════════════════════ -->

    <Style x:Key="PhantomButton" TargetType="Button">
        <Setter Property="Foreground" Value="#FF00F0FF"/>
        <Setter Property="FontFamily" Value="{StaticResource MonoFont}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Padding" Value="14,8"/>
        <Setter Property="Background" Value="#1200F0FF"/>
        <Setter Property="BorderBrush" Value="#4200F0FF"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            ClipToBounds="True">
                        <Grid>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              TextBlock.Foreground="{TemplateBinding Foreground}"/>
                            <!-- shimmer overlay -->
                            <Rectangle x:Name="Shimmer"
                                       Width="60"
                                       HorizontalAlignment="Left"
                                       IsHitTestVisible="False">
                                <Rectangle.RenderTransform>
                                    <TransformGroup>
                                        <SkewTransform AngleX="-18"/>
                                        <TranslateTransform x:Name="ShimmerTX" X="-80"/>
                                    </TransformGroup>
                                </Rectangle.RenderTransform>
                                <Rectangle.Fill>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="Transparent" Offset="0"/>
                                        <GradientStop Color="#22FFFFFF" Offset="0.5"/>
                                        <GradientStop Color="Transparent" Offset="1"/>
                                    </LinearGradientBrush>
                                </Rectangle.Fill>
                            </Rectangle>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetName="ShimmerTX"
                                                     Storyboard.TargetProperty="X"
                                                     From="-80" To="500"
                                                     Duration="0:0:3"
                                                     BeginTime="0:0:0.5"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="#2000F0FF"/>
                            <Setter TargetName="Root" Property="BorderBrush" Value="#FF00F0FF"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="#3000F0FF"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- INFERNO variant for STOP / destructive actions -->
    <Style x:Key="PhantomButtonInferno" TargetType="Button" BasedOn="{StaticResource PhantomButton}">
        <Setter Property="Foreground" Value="#FFFF3000"/>
        <Setter Property="Background" Value="#12FF3000"/>
        <Setter Property="BorderBrush" Value="#42FF3000"/>
    </Style>

    <!-- PULSE variant for START / live -->
    <Style x:Key="PhantomButtonPulse" TargetType="Button" BasedOn="{StaticResource PhantomButton}">
        <Setter Property="Foreground" Value="#FF00FF9D"/>
        <Setter Property="Background" Value="#1200FF9D"/>
        <Setter Property="BorderBrush" Value="#4200FF9D"/>
    </Style>

</ResourceDictionary>
